namespace TarefasStatus.Model.Enum
{
    public enum TarefaStatus
    {
        Criado = 1,
        EmProgresso = 2,
        Concluido = 3,
        Cancelado = 4
    }

    //classe enum que vai classificar cada status da tarefa.
}
